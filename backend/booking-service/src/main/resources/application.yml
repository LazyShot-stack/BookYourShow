spring:
  application:
    name: booking-service
  datasource:
    url: jdbc:postgresql://postgres:5432/booking_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 8083